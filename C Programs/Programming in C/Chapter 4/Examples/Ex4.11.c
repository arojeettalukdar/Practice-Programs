//Write a program to print the third power of 2 using pow() function.Assume the floating point number
#include<math.h>
void main()
{
	double x=2.0,y=3.0;

	printf("%lf raised to %lf is %lf\n",x,y,pow(x,y));
}
