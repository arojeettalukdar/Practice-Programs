// Write a program to calculate the area of the suqare whose area is given by the user
#include <stdio.h>
 int main()
{
    //area of square
    int side;
    printf("Enter side of the Square \n");
    scanf("%d",&side);

    printf(" The area of the square is: %d ",side* side);
    return 0;
}