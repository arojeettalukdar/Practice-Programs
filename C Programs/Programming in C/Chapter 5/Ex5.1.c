//Write a program to check the weather the entered number is less than 10. If yes, display the same
#include<stdio.h>
void main()
{
    int v;
    printf("Enter the number :");
    scanf("%d",&v);
    if(v<10);
    printf("\nNumber entered is less than 10");
    sleep(2); /*process halts for given value in seconds */

}